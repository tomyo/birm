export * from "./Button";

// // require all files inside this folder
// const req = require.context('./', true, /.js$/);

// // get all modules names
// const modules = req.keys().map(req);
// console.log(modules)

// // export them all
// module.exports = modules;
